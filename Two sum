class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        a = len(nums)
        for j in range(a-1):
            for k in range(j+1,a):
                if nums[j] + nums[k] == target:
                    return[j,k] 
        return[]
